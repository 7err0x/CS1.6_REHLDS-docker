// First boot: respawn mapcycle + DM preset (+exec runs before the first map).
// Live mode switch: rcon amx_respawn_ffa | rcon amx_respawn_tdm | rcon amx_respawn_apply

mapcyclefile mapcycle.txt
exec config/respawn-dm.cfg

echo "[config/gamemode-respawn.cfg] Respawn profile boot complete."
