; Permanent IP bans (engine loads this at map start).
; Host path: ./data/cs16-state/listip.cfg — bind-mounted into the container.
; After live IP bans: rcon writeip
;
; Format: addip <minutes> <ip>
;   minutes 0 = permanent

